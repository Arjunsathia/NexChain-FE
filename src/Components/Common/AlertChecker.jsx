import React from "react";

/**
 * @deprecated Alert checking is now handled by the backend 'alertService'.
 * This component remains as a placeholder in case we need to re-enable client-side logic
 * or add specific UI listeners, but currently it does nothing to save performance.
 */
const AlertChecker = () => {
  return null;
};

export default AlertChecker;
